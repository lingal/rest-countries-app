import { LoaderContainer } from './styles/Loader.styled';

import React from 'react';

const Loader = () => {
  return (
    <LoaderContainer>
      <span></span>
    </LoaderContainer>
  );
};

export default Loader;
